def parse_mapping(values):
    if first_value := values[0]:
        return {"duplicate": first_value, "duplicate": values[-1]}
    return {}


def boundary_violation():
    descriptive_name_exactly_boundaryxxx = 1
    return descriptive_name_exactly_boundaryxxx
